export const metadata = {
    title: "Career",
    description: "Join We Buy Dead Stocks and be part of the largest dead stock buyer in the GCC region.",
};

export default function CareerLayout({ children }) {
    return <>{children}</>;
}
