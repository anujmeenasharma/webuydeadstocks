export const metadata = {
    title: "Contact Us",
    description: "Get in touch with We Buy Dead Stocks for inquiries and evaluations.",
};

export default function ContactLayout({ children }) {
    return <>{children}</>;
}
