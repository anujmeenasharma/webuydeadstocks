export const metadata = {
    title: "Environment",
    description: "Learn about We Buy Dead Stocks' commitment to sustainability and eco-friendly practices.",
};

export default function EnvironmentLayout({ children }) {
    return <>{children}</>;
}
